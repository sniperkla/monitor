# 2026-09-04

- Renamed boot-phase `warp` → `handshake` in `src/app/page.client.js` (state machine, ref `handshakeFinishedRef`, `handleHandshakeComplete`, render block `key="handshake"`). Also updated comment in `src/components/landing/index.js`. Boot flow now: `boot → handshake → desktop`. The literal "warp star-trails" visual naming in `BootSequence.js` was intentionally left as-is (describes the animation, not the phase).
- Decision: `BootSequence` (boot) and `HandshakeTransition` (handshake) are NOT duplicate — boot = diagnostics/health-check, handshake = SSH connect tunnel. The only overlap was BootSequence's internal "warp star-trail" launch exit, which echoed HandshakeTransition's tunnel. Removed the warp streak lines + `launch-streak` keyframes + "warp" comments from `BootSequence.js`; the remaining glitch-dissolve (rgb-split/corrupt/tear bands) is distinct and kept. Verified with @babel/parser: PARSE OK.
- Request C: the "hyperwarp" the user saw behind the handshake was the `DataStreamCanvas` perspective-tunnel (hex-byte volume + forward-glow tunnel mouth, glitch bands, vignette, shake/roll) that `HandshakeTransition.js` was rendering as the SSH metaphor. Removed it for a clean terminal-on-black handshake. Changes in `HandshakeTransition.js`: import narrowed to `prefersReducedMotion`; dropped `DataStreamCanvas`, `useSmoothedPointer`, `easeOutCubic`, `easeInOutSine`, `pointer`; `handleFrame(elapsed)` now timeline-driven (no `ds.state`/`s.*` tunnel driving); driver `useEffect` switched to a standalone `requestAnimationFrame` loop calling `handleFrame(Date.now() - t0)` with audio + safety timeout; `<DataStreamCanvas>` JSX removed, terminal `<div ref={stageRef}>` is now direct child of the `motion.div`. Verified PARSE OK (babel), ESLint 0 errors/warnings, no dangling refs via Grep. NOTE: `DataStreamCanvas` is still used by `RevealScreen.js` (guest login idle packet drift) — intentionally retained there.
- Request D: renamed boot phase `boot` → `preflight` (user said "depend on you", recommended `preflight`). In `src/app/page.client.js`: phase literal `'boot'`→`'preflight'`; state var `bootPhase`/`setBootPhase`→`flowPhase`/`setFlowPhase` (now spans preflight/handshake/desktop); prop `bootPhase={flowPhase}` passed to `DesktopEnvironment` unchanged (kept prop name so `DesktopEnvironment.js` needs no edit). CRITICAL gotcha: the rename also caught a latent bug — the handshake `AnimatePresence` block at old line 132 still referenced the old `bootPhase` variable; fixed to `flowPhase`. Default ESLint config does NOT enable `no-undef`, so this would have slipped through; verified with `eslint --rule '{"no-undef":"error"}'` → clean. Boot flow is now `preflight → handshake → desktop`. The `type: 'boot'` log-line tags in `BootSequence.js`/`BootScreen.js` are an unrelated line-classification (POST/[BOOT] styling), NOT the phase — left untouched.
