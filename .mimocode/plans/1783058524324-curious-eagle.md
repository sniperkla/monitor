# Move Backup History to Jobs Tab

## Problem
Backup history is currently in the Backup tab, only visible after a backup completes. Users expect to find past backups in the Jobs tab.

## Changes

### `src/apps/ServerBackupApp.js`

1. **Remove** the backup history section from the Backup tab (lines 648-696)
2. **Add** the backup history section to the Jobs tab, alongside the existing `ServerFilesList`
3. Show history **even when empty** with a helpful message like "No backup history yet"
4. Add a **Download** button (currently a small icon — make it more visible with text label)

## Files to modify
- `src/apps/ServerBackupApp.js` — move history section from Backup tab to Jobs tab

## Verification
1. Go to Jobs tab — confirm backup history is visible
2. Run a backup — confirm it appears in Jobs tab history
3. Click download — confirm it works
4. Remove entry — confirm it works
