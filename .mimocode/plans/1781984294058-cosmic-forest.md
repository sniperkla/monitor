# Plan: Compact Logo (icon only, no text)

## Changes

### 1. `src/components/landing/RevealScreen.js` — LogoWithPulseRings
- Remove the "Text content" div (lines 391-412) that has "SSH", "Monitor", subtitle, and "online" dot
- Remove `gap-5` from container, use smaller padding `px-4 py-3`
- Keep: terminal icon block (`>_` cursor + green corner dot), glassmorphism container, ambient glow, animated border accent

### 2. `src/components/Desktop/DesktopEnvironment.js` — Boot splash logo
- Remove the "Text" div (gradient "SSH Monitor" + subtitle + "online" dot)
- Remove `gap-5`, use smaller padding `px-4 py-3`
- Keep: terminal icon block, green corner dot, blinking cursor

## Verification
- Landing page: logo shows just the `>_` icon in glassmorphism container
- Boot splash: same compact icon
- Hover effects still work
