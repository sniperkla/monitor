# CLAUDE.md

The handoff document for this repo is **[AGENTS.md](./AGENTS.md)**. Read it first — it is
written for any compatible agent and covers the agent Web UI architecture, the
non-obvious rules that broke things, the test setup, and the open items.

Repo is at `~/Desktop/monitor`. Dev server: `npm run dev` (port 3030). Tests: `npm test`
(571 passing). Production is `monitor.eaqdragon.com` and does **not** auto-deploy.

This file is deliberately just a pointer so there is one source of truth. Do not
duplicate AGENTS.md content here.
