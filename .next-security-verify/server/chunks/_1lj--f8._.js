module.exports=[94775,e=>e.a(async(t,n)=>{try{var o=e.i(89171),r=e.i(23667),s=e.i(80533),i=e.i(46589),a=e.i(40317),l=e.i(54981),c=e.i(47185),u=e.i(73757),d=e.i(37034),p=t([s,i,c]);function E(e){if(!e)return"0 2 * * *";if(5===e.trim().split(/\s+/).length)return e.trim();switch(e){case"hourly":return"0 * * * *";case"daily":default:return"0 2 * * *";case"weekly":return"0 2 * * 0"}}function _(e){if(!e)return"Daily";let t=E(e),n=t.trim().split(/\s+/);if(5===n.length){let[e,t,o,r,s]=n;if("*/5"===e&&"*"===t&&"*"===o&&"*"===r&&"*"===s)return"Every 5 Minutes";if("*/15"===e&&"*"===t&&"*"===o&&"*"===r&&"*"===s)return"Every 15 Minutes";if("*/30"===e&&"*"===t&&"*"===o&&"*"===r&&"*"===s)return"Every 30 Minutes";if("0"===e&&"*"===t&&"*"===o&&"*"===r&&"*"===s)return"Every Hour at :00";if("*"===o&&"*"===r&&"*"===s&&!e.includes("/")&&!t.includes("/")){let n=parseInt(t,10),o=parseInt(e,10);return`Every Day at ${String(n%12==0?12:n%12).padStart(2,"0")}:${String(o).padStart(2,"0")} ${n>=12?"PM":"AM"}`}}switch(e){case"hourly":return"Every Hour at :00";case"daily":return"Every Day at 02:00 AM";case"weekly":return"Every Sunday at 02:00 AM";default:return`Cron (${t})`}}function h(e){return`'${String(e||"").replace(/'/g,"'\\''")}'`}async function R(e){try{if(!await (0,r.getServerSession)(s.authOptions))return o.NextResponse.json({success:!1,error:"Unauthorized"},{status:401});let{searchParams:t}=new URL(e.url),n=t.get("jobId"),i=t.get("targetSshConnId"),a="1"===t.get("fetchLogs");if(!n||!i)return o.NextResponse.json({success:!1,error:"jobId and targetSshConnId are required"},{status:400});let l=await (0,c.getSshConfig)(i),u=n.replace(/[^a-zA-Z0-9_-]/g,"_"),d=`
CRON_LINE=$(crontab -l 2>/dev/null | grep -F "mongosync-${u}" || true)
echo "CRON_LINE:$CRON_LINE"
LOG_DIR="$HOME/.mongosync-scripts/logs"
if [ -d "$LOG_DIR" ]; then
  LATEST=$(ls -t "$LOG_DIR"/mongosync-${u}-*.log 2>/dev/null | head -1)
  if [ -n "$LATEST" ]; then
    echo "LATEST_LOG:$LATEST"
    echo "LOG_TAIL_START"
    tail -20 "$LATEST" 2>/dev/null
    echo "LOG_TAIL_END"
  fi
fi
`,p=(await (0,c.execCommand)(l,d)).stdout||"",E=p.match(/^CRON_LINE:(.*)/m),_=E?E[1].trim():null,h=p.match(/^LATEST_LOG:(.*)/m),R=h?h[1].trim():null,m=null;if(R){let e=R.match(/(\d{8}_\d{6})\.log$/);if(e){let t=e[1],n=t.slice(0,4),o=t.slice(4,6),r=t.slice(6,8),s=t.slice(9,11),i=t.slice(11,13),a=t.slice(13,15);m=new Date(`${n}-${o}-${r}T${s}:${i}:${a}`).toISOString()}}let $=null,g=p.match(/LOG_TAIL_START\n([\s\S]*?)LOG_TAIL_END/);return g&&($=g[1].trim()),o.NextResponse.json({success:!0,cronLine:_||null,installed:!!_,lastRunFromLog:m,latestLogFile:R,logTail:a?$:void 0})}catch(e){return o.NextResponse.json({success:!1,error:e.message},{status:500})}}async function m(e){try{let n,p=await (0,r.getServerSession)(s.authOptions);if(!p)return o.NextResponse.json({success:!1,error:"Unauthorized"},{status:401});let R=p.user?.id;if(!R)return o.NextResponse.json({success:!1,error:"User ID not found in session"},{status:400});let{jobId:m,jobName:$,schedule:g,targetSshConnId:T,connectionId:f,database:O,collection:S,driveFolderId:y}=await e.json();if(!m||!T||!f||!O||!y||!g||"manual"===g)return o.NextResponse.json({success:!1,error:"jobId, targetSshConnId, connectionId, database, driveFolderId and a non-manual schedule are required"},{status:400});let I=await (0,i.default)(),C=new l.ConnectionRepository(I);await C.init();let v=new a.SystemSettingRepository(I,R);if(await v.init(),"default"===f)n=process.env.MONGODB_URI||"mongodb://localhost:27017/monitor";else{var t;let e,r,s,i,a,l=await C.findById(f);if(!l)return o.NextResponse.json({success:!1,error:"MongoDB connection not found"},{status:404});if(l.userId&&String(l.userId)!==String(R))return o.NextResponse.json({success:!1,error:"Access denied: this connection belongs to another user"},{status:403});r=(t=l.toObject?l.toObject():l).password?(0,u.decrypt)(t.password):"",i=(s=t.isSrv||t.host&&t.host.includes(".mongodb.net"))?"mongodb+srv":"mongodb",a=s||!t.port?"":`:${t.port}`,e=t.username&&r?`${i}://${t.username}:${encodeURIComponent(r)}@${t.host}${a}/${t.database||""}`:`${i}://${t.host}${a}/${t.database||""}`,t.authSource&&(e+=`?authSource=${t.authSource}`),n=e}n=function(e){if(!e)return e;try{if(e.startsWith("mongodb+srv://"))return e;let t=e.match(/(@|\/\/)((?!127\.0\.0\.1$|localhost$)[a-zA-Z][a-zA-Z0-9_-]*)(:(\d+))/);if(!t)return e;let n=t[2],o=t[4],r=process.env.DOCKER_MONGO_HOST_PORT||"27021",s=e.replace(`${n}:${o}`,`127.0.0.1:${r}`);return logger.info(`[mongo-sync/cron] Rewrote Docker URI: ${n}:${o} → 127.0.0.1:${r}`),s}catch{return e}}(n);let L=await v.findOne({key:"google_drive_config"}),D=L?.value;if(!D?.refreshToken)return o.NextResponse.json({success:!1,error:"Google Drive not connected. Please connect Google Drive first."},{status:400});let{refreshToken:A,clientId:P,clientSecret:N}=D,M=await (0,c.getSshConfig)(T),b=m.replace(/[^a-zA-Z0-9_-]/g,"_"),w=($||m).replace(/[^a-zA-Z0-9_.-]/g,"_"),U=E(g),x=["*","ALL_COLLECTIONS","All Collections","All Collections (*)"].includes(S),F=P||process.env.GOOGLE_CLIENT_ID||"",j=N||process.env.GOOGLE_CLIENT_SECRET||"",k=["#!/bin/bash",`# MongoSync Auto-Backup: ${w}`,`# Job ID: ${m}`,"\n# Use set -u only: -e (exit on error) would kill the script before PYEXIT=$? is captured.\n# -u catches unset variable bugs. All critical exit codes are checked explicitly.\nset -u\n\n# ── Embedded config (set at install time) ───",`MONGO_URI=${h(n)}`,`DB_NAME=${h(O)}`,`COLLECTION=${h(S)}`,`IS_ALL_COLLECTIONS=${x?"true":"false"}`,`GDRIVE_FOLDER_ID=${h(y)}`,`GDRIVE_REFRESH_TOKEN=${h(A)}`,`GDRIVE_CLIENT_ID=${h(F)}`,`GDRIVE_CLIENT_SECRET=${h(j)}`,'\n# ── Paths ───\nSCRIPTS_DIR="$HOME/.mongosync-scripts"\nLOGS_DIR="$SCRIPTS_DIR/logs"\nTMP_DIR="$SCRIPTS_DIR/tmp"\nmkdir -p "$LOGS_DIR" "$TMP_DIR"\n\nTIMESTAMP=$(date +%Y%m%d_%H%M%S)','LOG="$LOGS_DIR/mongosync-'+b+'-$TIMESTAMP.log"','LOCKFILE="$SCRIPTS_DIR/lock-'+b+'.lock"','\n# ── Redirect ALL stdout+stderr to log from this point on ───\n# This ensures any crash or error is always captured in the log file.\nexec >> "$LOG" 2>&1\n','echo "=== MongoSync Start: '+w+' | $TIMESTAMP ==="','\n# Log sanitized connection info for debugging (strip password)\n_SANITIZED_URI=$(echo "$MONGO_URI" | sed "s#://[^:]*:[^@]*@#://***:***@#" || echo "(uri parse error)")\necho "$(date): MongoDB URI: $_SANITIZED_URI"\necho "$(date): Database: $DB_NAME"\n\n# ── Lock (per-job) ───\nif command -v flock > /dev/null 2>&1; then\n  exec 9>"$LOCKFILE"\n  flock -n 9 || { echo "$(date): Already running, skipping."; exit 0; }\nelse','  LOCKDIR="$SCRIPTS_DIR/lock-'+b+'.lockdir"','  mkdir "$LOCKDIR" 2>/dev/null || { echo "$(date): Already running."; exit 0; }\n  trap \'rm -rf "$LOCKDIR"\' EXIT\nfi\n\n# ── Global lock (prevent parallel jobs from competing for resources) ───\nGLOBAL_LOCK="$SCRIPTS_DIR/global.lock"\nif command -v flock > /dev/null 2>&1; then\n  exec 8>"$GLOBAL_LOCK"\n  flock -w 300 8 || { echo "$(date): Another mongosync job is still running after 5 min wait, skipping."; exit 0; }\nfi\n\n# ── Disk space guard (abort if disk >= 85% full) ───\nDISK_PCT=$(df "$TMP_DIR" 2>/dev/null | awk \'NR==2 {gsub(/%/,""); print $5}\' || echo "")\nif [ -n "$DISK_PCT" ] && [ "$DISK_PCT" -ge 85 ] 2>/dev/null; then\n  echo "$(date): ERROR: Disk is $DISK_PCT% full (threshold 85%). Aborting to protect server."\n  exit 1\nfi\necho "$(date): Disk usage: ${DISK_PCT:-(unknown)}% — OK"\n\n# ── Step 1: Refresh OAuth token ───\n# Use a temp file to avoid python3 pipeline exit code killing the script\n_OAUTH_RESP_FILE="$TMP_DIR/oauth_resp_$TIMESTAMP.json"\ncurl -s -X POST "https://oauth2.googleapis.com/token" \\\n  --data-urlencode "client_id=$GDRIVE_CLIENT_ID" \\\n  --data-urlencode "client_secret=$GDRIVE_CLIENT_SECRET" \\\n  --data-urlencode "refresh_token=$GDRIVE_REFRESH_TOKEN" \\\n  --data-urlencode "grant_type=refresh_token" \\\n  > "$_OAUTH_RESP_FILE" 2>/dev/null || true\nACCESS_TOKEN=$(python3 -c "import sys,json; print(json.load(open(sys.argv[1])).get(\'access_token\',\'\'))" "$_OAUTH_RESP_FILE" 2>/dev/null || echo "")\nrm -f "$_OAUTH_RESP_FILE"\n\nif [ -z "$ACCESS_TOKEN" ]; then\n  echo "$(date): ERROR: Failed to get OAuth token. Check Google Drive credentials."\n  exit 1\nfi\necho "$(date): OAuth token obtained."\n\n# ── Step 2: Ensure python3 + pymongo are available ───\nif ! command -v python3 > /dev/null 2>&1; then\n  echo "$(date): ERROR: python3 is not installed. Cannot continue."\n  exit 1\nfi\necho "$(date): python3 found: $(python3 --version 2>&1)"\nif ! python3 -c "import pymongo" > /dev/null 2>&1; then\n  echo "$(date): pymongo not found — installing..."\n  python3 -m pip install --user --quiet pymongo 2>&1 || \\\n    pip3 install --user --quiet pymongo 2>&1 || true\n  if ! python3 -c "import pymongo" > /dev/null 2>&1; then\n    echo "$(date): ERROR: pymongo install failed. Please run: pip3 install pymongo"\n    exit 1\n  fi\nfi\necho "$(date): pymongo ready: $(python3 -c "import pymongo; print(pymongo.version)" 2>/dev/null)"\n\n# ── Step 3: Create timestamped subfolder in GDrive ───\nFOLDER_DATE=$(date +"%Y-%m-%d_%H-%M")\n# Write JSON body to a temp file to avoid all shell quoting/expansion issues\n_SUBFOLDER_JSON_FILE="$TMP_DIR/subfolder_meta_$TIMESTAMP.json"\n_SUBFOLDER_RESP_FILE="$TMP_DIR/subfolder_resp_$TIMESTAMP.json"\npython3 -c "import json,sys; open(sys.argv[1],\'w\').write(json.dumps({\'name\':sys.argv[2],\'mimeType\':\'application/vnd.google-apps.folder\',\'parents\':[sys.argv[3]]}))" "$_SUBFOLDER_JSON_FILE" "$FOLDER_DATE" "$GDRIVE_FOLDER_ID" 2>/dev/null || true\ncurl -s -X POST "https://www.googleapis.com/drive/v3/files" \\\n  -H "Authorization: Bearer $ACCESS_TOKEN" \\\n  -H "Content-Type: application/json" \\\n  -d @"$_SUBFOLDER_JSON_FILE" \\\n  > "$_SUBFOLDER_RESP_FILE" 2>/dev/null || true\nrm -f "$_SUBFOLDER_JSON_FILE"\nSUBFOLDER_ID=$(python3 -c "import sys,json; print(json.load(open(sys.argv[1])).get(\'id\',\'\'))" "$_SUBFOLDER_RESP_FILE" 2>/dev/null || echo "")\nif [ -z "$SUBFOLDER_ID" ]; then\n  _SUBFOLDER_ERR=$(python3 -c "import sys,json; d=json.load(open(sys.argv[1])); print(d.get(\'error\',{}).get(\'message\',\'unknown\'))" "$_SUBFOLDER_RESP_FILE" 2>/dev/null || echo "parse error")\n  echo "$(date): WARNING: Could not create subfolder ($_SUBFOLDER_ERR), uploading to root folder."\n  SUBFOLDER_ID="$GDRIVE_FOLDER_ID"\nfi\nrm -f "$_SUBFOLDER_RESP_FILE"\necho "$(date): Subfolder ready: $FOLDER_DATE ($SUBFOLDER_ID)"\n\n# ── Step 4: Upload function with retry logic (uploads into the timestamped subfolder) ───\nupload_file() {\n  local DUMP_FILE="$1"\n  local FILENAME="$2"\n  local PARENT_ID="$3"\n  local MAX_RETRIES=3\n  local RETRY_COUNT=0\n  \n  echo "$(date): Uploading $FILENAME ($(du -sh "$DUMP_FILE" 2>/dev/null | cut -f1)) ..."\n  \n  # Write metadata JSON to temp file to avoid shell quoting issues\n  _META_FILE="/tmp/gdrive_meta_$$.json"\n  python3 -c "import json,sys; open(sys.argv[1],\'w\').write(json.dumps({\'name\':sys.argv[2],\'parents\':[sys.argv[3]]}))" "$_META_FILE" "$FILENAME" "$PARENT_ID" 2>/dev/null || true\n  \n  # Retry loop with exponential backoff\n  while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do\n    HTTP_CODE=$(curl -s -o /tmp/gdrive_up_$$.json -w "%{http_code}" \\\n      -X POST "https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart" \\\n      -H "Authorization: Bearer $ACCESS_TOKEN" \\\n      -F "metadata=@$_META_FILE;type=application/json;charset=UTF-8" \\\n      -F "file=@$DUMP_FILE;type=application/json" 2>/dev/null || echo "000")\n    \n    if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "201" ]; then\n      echo "$(date): ✅ Uploaded: $FILENAME"\n      rm -f "$_META_FILE" /tmp/gdrive_up_$$.json\n      return 0\n    fi\n    \n    RETRY_COUNT=$((RETRY_COUNT + 1))\n    if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then\n      WAIT_TIME=$((5 * RETRY_COUNT))\n      echo "$(date): ⚠️  Upload failed HTTP $HTTP_CODE, retrying in ${WAIT_TIME}s (attempt $((RETRY_COUNT + 1))/$MAX_RETRIES)..."\n      sleep $WAIT_TIME\n    else\n      echo "$(date): ❌ Upload failed after $MAX_RETRIES attempts, HTTP $HTTP_CODE: $(cat /tmp/gdrive_up_$$.json 2>/dev/null)"\n    fi\n  done\n  \n  rm -f "$_META_FILE" /tmp/gdrive_up_$$.json\n  return 1\n}\n\n\n# ── Step 5: Export all data via pymongo (single connection) ───\n# Write the Python export script to a temp file\nPYEXPORT_SCRIPT="$TMP_DIR/pyexport_$TIMESTAMP.py"\n# Use a job-specific export dir so files from different jobs never mix','EXPORT_DIR="$TMP_DIR/export_'+b+'_$TIMESTAMP"','mkdir -p "$EXPORT_DIR"\ncat > "$PYEXPORT_SCRIPT" << \'PYEOF\'\nimport sys, os, json\nfrom datetime import datetime, timezone\nfrom bson import ObjectId, Decimal128\nfrom pymongo import MongoClient\n\ndef default_serializer(o):\n    if isinstance(o, ObjectId): return str(o)\n    if isinstance(o, datetime): return o.isoformat()\n    if isinstance(o, Decimal128): return str(o)\n    if isinstance(o, bytes): return o.hex()\n    return str(o)\n\nmongo_uri    = sys.argv[1]\ndb_name      = sys.argv[2]\ncollection   = sys.argv[3]   # single collection name, or "__ALL__"\nout_dir      = sys.argv[4]\n\ndef log(msg):\n    ts = datetime.now().strftime("%a %b %e %H:%M:%S %Z %Y")\n    print(f"{ts}: {msg}", flush=True)\n\nclient = MongoClient(mongo_uri, serverSelectionTimeoutMS=10000)\ndb = client[db_name]\n\nif collection == "__ALL__":\n    names = sorted(n for n in db.list_collection_names() if not n.startswith("system."))\nelse:\n    names = [collection]\n\ntotal = len(names)\nsuccess = 0\nfailed  = 0\n\nfor name in names:\n    out_path = os.path.join(out_dir, name + ".json")\n    try:\n        docs = list(db[name].find({}))\n        with open(out_path, "w") as f:\n            json.dump(docs, f, default=default_serializer, ensure_ascii=False)\n        log(f"exported {name} ({len(docs)} docs) → {out_path}")\n        success += 1\n    except Exception as e:\n        log(f"ERROR exporting {name}: {e}")\n        failed += 1\n\nclient.close()\nlog(f"PYEXPORT_DONE total={total} success={success} failed={failed}")\nsys.exit(0 if failed == 0 else 1)\nPYEOF\n\n# Determine which collection(s) to export\nif [ "$IS_ALL_COLLECTIONS" = "true" ]; then\n  EXPORT_TARGET="__ALL__"\nelse\n  EXPORT_TARGET="$COLLECTION"\nfi\n\necho "$(date): Starting pymongo export for: $EXPORT_TARGET"\n# Capture python3 exit code without triggering set -e (use && ... || ... pattern)\npython3 "$PYEXPORT_SCRIPT" "$MONGO_URI" "$DB_NAME" "$EXPORT_TARGET" "$EXPORT_DIR" && PYEXIT=0 || PYEXIT=$?\nrm -f "$PYEXPORT_SCRIPT"\n\nif [ "$PYEXIT" -ne 0 ]; then\n  echo "$(date): ERROR: pymongo export failed (exit $PYEXIT)."\n  rm -rf "$EXPORT_DIR"\n  exit 1\nfi\n\n# Upload each exported JSON file to the Drive subfolder\nTOTAL_COUNT=0\nSUCCESS_COUNT=0\nFAILED_COUNT=0\nfor DUMP_FILE in "$EXPORT_DIR"/*.json; do\n  [ -f "$DUMP_FILE" ] || continue\n  FILENAME=$(basename "$DUMP_FILE")\n  TOTAL_COUNT=$((TOTAL_COUNT + 1))\n  if upload_file "$DUMP_FILE" "$FILENAME" "$SUBFOLDER_ID"; then\n    SUCCESS_COUNT=$((SUCCESS_COUNT + 1))\n  else\n    FAILED_COUNT=$((FAILED_COUNT + 1))\n  fi\ndone\nrm -rf "$EXPORT_DIR"\n\necho ""\necho "=== Summary | $(date) ==="\nif [ $TOTAL_COUNT -gt 0 ]; then\n  echo "Total collections: $TOTAL_COUNT"\n  echo "✅ Successfully uploaded: $SUCCESS_COUNT"\n  echo "❌ Failed: $FAILED_COUNT"\n  SUCCESS_RATE=$((SUCCESS_COUNT * 100 / TOTAL_COUNT))\n  echo "Success rate: ${SUCCESS_RATE}%"\nfi\necho "=== Done | $(date) ==="','find "$LOGS_DIR" -name "mongosync-'+b+'-*.log" -mtime +14 -delete 2>/dev/null || true'].join("\n"),H=`$HOME/.mongosync-scripts/mongosync-${b}.sh`,G=`${U} /bin/bash ${H}`,B=`
mkdir -p "$HOME/.mongosync-scripts/logs" "$HOME/.mongosync-scripts/tmp"
chmod 700 "$HOME/.mongosync-scripts" "$HOME/.mongosync-scripts/logs" "$HOME/.mongosync-scripts/tmp" 2>/dev/null || true
cat <<'SCRIPT_EOF' > "$HOME/.mongosync-scripts/mongosync-${b}.sh"
${k}
SCRIPT_EOF
chmod 700 "$HOME/.mongosync-scripts/mongosync-${b}.sh"
# Syntax check before installing crontab
SYNTAX_ERR=$(bash -n "$HOME/.mongosync-scripts/mongosync-${b}.sh" 2>&1)
if [ -n "$SYNTAX_ERR" ]; then
  echo "SYNTAX_ERROR: $SYNTAX_ERR"
  exit 1
fi
# ── Preflight probe: run environment checks now so errors appear in log immediately ──
# This writes a log entry right at install time — if anything is missing, the log shows why.
PROBE_LOG="$HOME/.mongosync-scripts/logs/mongosync-${b}-$(date +%Y%m%d_%H%M%S).log"
{
  echo "=== MongoSync Preflight Probe: ${b} | $(date) ==="
  echo "$(date): Checking environment..."
  # Check python3
  if command -v python3 > /dev/null 2>&1; then
    echo "$(date): ✅ python3: $(python3 --version 2>&1)"
  else
    echo "$(date): ❌ ERROR: python3 not found — cron job will fail. Install python3 first."
  fi
  # Check pymongo
  if python3 -c "import pymongo" > /dev/null 2>&1; then
    echo "$(date): ✅ pymongo: $(python3 -c 'import pymongo; print(pymongo.version)' 2>/dev/null)"
  else
    echo "$(date): ❌ ERROR: pymongo not installed — run: pip3 install pymongo"
  fi
  # Check curl
  if command -v curl > /dev/null 2>&1; then
    echo "$(date): ✅ curl: $(curl --version 2>&1 | head -1)"
  else
    echo "$(date): ❌ ERROR: curl not found — cron job will fail. Install curl first."
  fi
  # Check MongoDB reachability (quick 3s timeout)
  MONGO_REACHABLE=$(python3 -c "
from pymongo import MongoClient
import sys
import { logger } from '@/lib/logger';
try:
    c = MongoClient('${n.replace(/'/g,"'\\''")}', serverSelectionTimeoutMS=3000)
    c.admin.command('ping')
    print('ok')
    c.close()
except Exception as e:
    print('fail: ' + str(e))
" 2>/dev/null || echo "fail: python3 error")
  if [ "$MONGO_REACHABLE" = "ok" ]; then
    echo "$(date): ✅ MongoDB reachable"
  else
    echo "$(date): ❌ ERROR: MongoDB not reachable — $MONGO_REACHABLE"
  fi
  echo "$(date): Preflight probe complete."
  echo "=== Cron schedule: ${G} ==="
} >> "$PROBE_LOG" 2>&1
echo "PROBE_LOG:$PROBE_LOG"
TMP_CRON=$(mktemp 2>/dev/null || echo "/tmp/mongosync_cron_tmp_$$")
crontab -l 2>/dev/null | grep -F -v "mongosync-${b}" > "$TMP_CRON" || true
echo ${h(G)} >> "$TMP_CRON"
crontab "$TMP_CRON" 2>&1 || true
rm -f "$TMP_CRON"
echo "INSTALLED_SUCCESS"
`,X=await (0,c.execCommand)(M,B);if(0!==X.code||!X.stdout.includes("INSTALLED_SUCCESS")){let e=X.stdout.match(/SYNTAX_ERROR: (.+)/)?.[1],t=e?`Script syntax error: ${e}`:X.stderr.trim()||X.stdout.trim()||"Failed to install script on target SSH server";return logger.error("[mongo-sync/cron POST] SSH exec failure:",t),o.NextResponse.json({success:!1,error:t,stdout:X.stdout,stderr:X.stderr},{status:500})}let K=X.stdout.match(/^PROBE_LOG:(.+)$/m),Y=K?K[1].trim():null,q=null,z=[];if(Y)try{z=((q=(await (0,c.execCommand)(M,`cat ${(0,d.shellQuote)(Y)} 2>/dev/null || echo "(probe log not found)"`)).stdout.trim())||"").split("\n").filter(e=>e.includes("❌ ERROR:")).map(e=>e.replace(/^[^❌]*❌ ERROR:\s*/,"").trim())}catch(e){}return o.NextResponse.json({success:!0,message:`Schedule installed on SSH server (${_(g)})`,cronLine:G,humanSchedule:_(g),scriptPath:`~/.mongosync-scripts/mongosync-${b}.sh`,scriptInstalled:!0,probeLog:q,probeWarnings:z.length>0?z:void 0})}catch(e){return logger.error("[mongo-sync/cron POST] error:",e),o.NextResponse.json({success:!1,error:e.message},{status:500})}}async function $(e){try{if(!await (0,r.getServerSession)(s.authOptions))return o.NextResponse.json({success:!1,error:"Unauthorized"},{status:401});let{jobId:t,targetSshConnId:n}=await e.json();if(!t||!n)return o.NextResponse.json({success:!1,error:"jobId and targetSshConnId are required"},{status:400});let i=t.replace(/[^a-zA-Z0-9_-]/g,"_"),a=await (0,c.getSshConfig)(n),l=`
TMP_CRON=$(mktemp)
crontab -l 2>/dev/null | grep -F -v "mongosync-${i}" > "$TMP_CRON" || true
crontab "$TMP_CRON"
rm -f "$TMP_CRON"
rm -f "$HOME/.mongosync-scripts/mongosync-${i}.sh"
echo "REMOVED"
`,u=await (0,c.execCommand)(a,l);return o.NextResponse.json({success:u.stdout.includes("REMOVED"),message:u.stdout.includes("REMOVED")?"Crontab entry and script removed from SSH server.":"Remove may have partially failed.",output:u.stdout})}catch(e){return logger.error("[mongo-sync/cron DELETE] error:",e),o.NextResponse.json({success:!1,error:e.message},{status:500})}}[s,i,c]=p.then?(await p)():p,e.s(["DELETE",0,$,"GET",0,R,"POST",0,m]),n()}catch(e){n(e)}},!1),98003,e=>{"use strict";var t=e.i(8970),n=e.i(74017),o=e.i(96250),r=e.i(59756),s=e.i(61916),i=e.i(74677),a=e.i(69741),l=e.i(16795),c=e.i(87718),u=e.i(95169),d=e.i(47587),p=e.i(66012),E=e.i(70101),_=e.i(26937),h=e.i(10372),R=e.i(93695);e.i(52474);var m=e.i(5232);let $=new t.AppRouteRouteModule({definition:{kind:n.RouteKind.APP_ROUTE,page:"/api/mongo-sync/cron/route",pathname:"/api/mongo-sync/cron",filename:"route",bundlePath:""},distDir:".next-security-verify",relativeProjectDir:"",resolvedPagePath:"[project]/src/app/api/mongo-sync/cron/route.js",nextConfigOutput:"",userland:()=>e.r(94775),...{}}),{workAsyncStorage:g,workUnitAsyncStorage:T,serverHooks:f}=$;async function O(e,t,o){o.requestMeta&&(0,r.setRequestMeta)(e,o.requestMeta),$.isDev&&(0,r.addRequestMeta)(e,"devRequestTimingInternalsEnd",process.hrtime.bigint());let g="/api/mongo-sync/cron/route";g=g.replace(/\/index$/,"")||"/";let T=await $.prepare(e,t,{srcPage:g,multiZoneDraftMode:!1});if(!T)return t.statusCode=400,t.end("Bad Request"),null==o.waitUntil||o.waitUntil.call(o,Promise.resolve()),null;let{buildId:f,deploymentId:O,params:S,nextConfig:y,parsedUrl:I,isDraftMode:C,prerenderManifest:v,routerServerContext:L,isOnDemandRevalidate:D,revalidateOnlyGenerated:A,resolvedPathname:P,clientReferenceManifest:N,serverActionsManifest:M}=T,b=(0,a.normalizeAppPath)(g),w=!!(v.dynamicRoutes[b]||v.routes[P]),U=async()=>((null==L?void 0:L.render404)?await L.render404(e,t,I,!1):t.end("This page could not be found"),null);if(w&&!C){let e=!!v.routes[P],t=v.dynamicRoutes[b];if(t&&!1===t.fallback&&!e){if(y.adapterPath)return await U();throw new R.NoFallbackError}}let x=null;!w||$.isDev||C||(x="/index"===(x=P)?"/":x);let F=!0===$.isDev||!w,j=w&&!F;M&&N&&(0,i.setManifestsSingleton)({page:g,clientReferenceManifest:N,serverActionsManifest:M});let k=e.method||"GET",H=(0,s.getTracer)(),G=H.getActiveScopeSpan(),B=!!(null==L?void 0:L.isWrappedByNextServer),X=!!(0,r.getRequestMeta)(e,"minimalMode"),K=(0,r.getRequestMeta)(e,"incrementalCache")||await $.getIncrementalCache(e,y,v,X);null==K||K.resetRequestCache(),globalThis.__incrementalCache=K;let Y={params:S,previewProps:v.preview,renderOpts:{experimental:{authInterrupts:!!y.experimental.authInterrupts,useCacheTimeout:y.experimental.useCacheTimeout},cacheComponents:!!y.cacheComponents,validationLevel:y.experimental.instantInsights.validationLevel,supportsDynamicResponse:F,incrementalCache:K,hmrRefreshHash:(0,r.getRequestMeta)(e,"hmrRefreshHash"),cacheLifeProfiles:y.cacheLife,staticPageGenerationTimeout:y.staticPageGenerationTimeout,waitUntil:o.waitUntil,onClose:e=>{t.on("close",e)},onAfterTaskError:void 0,onInstrumentationRequestError:(t,n,o,r)=>$.onRequestError(e,t,o,r,L)},sharedContext:{buildId:f,deploymentId:O}},q=new l.NodeNextRequest(e),z=new l.NodeNextResponse(t),V=c.NextRequestAdapter.fromNodeNextRequest(q,(0,c.signalFromNodeResponse)(t)),W=async({previousCacheEntry:n})=>{try{if(!X&&D&&A&&!n)return t.statusCode=404,t.setHeader("x-nextjs-cache","REVALIDATED"),t.end("This page could not be found"),null;let r=await $.handle(V,Y);e.fetchMetrics=Y.renderOpts.fetchMetrics;let s=Y.renderOpts.pendingWaitUntil;s&&o.waitUntil&&(o.waitUntil(s),s=void 0);let i=Y.renderOpts.collectedTags;if(!w)return await (0,p.sendResponse)(q,z,r,s),null;{let e=await r.blob(),t=(0,E.toNodeOutgoingHttpHeaders)(r.headers);i&&(t[h.NEXT_CACHE_TAGS_HEADER]=i),!t["content-type"]&&e.type&&(t["content-type"]=e.type);let n=void 0!==Y.renderOpts.collectedRevalidate&&!(Y.renderOpts.collectedRevalidate>=h.INFINITE_CACHE)&&Y.renderOpts.collectedRevalidate,o=void 0===Y.renderOpts.collectedExpire||Y.renderOpts.collectedExpire>=h.INFINITE_CACHE?!1!==n&&n>0?y.expireTime:void 0:Y.renderOpts.collectedExpire;return{value:{kind:m.CachedRouteKind.APP_ROUTE,status:r.status,body:Buffer.from(await e.arrayBuffer()),headers:t},cacheControl:{revalidate:n,expire:o}}}}catch(t){throw(null==n?void 0:n.isStale)&&await $.onRequestError(e,t,{routerKind:"App Router",routePath:g,routeType:"route",revalidateReason:(0,d.getRevalidateReason)({isStaticGeneration:j,isOnDemandRevalidate:D})},!1,L),t}},Z=async(r,i)=>{try{var a,l;let r=await $.handleResponse({req:e,nextConfig:y,cacheKey:x,routeKind:n.RouteKind.APP_ROUTE,isFallback:!1,prerenderManifest:v,isRoutePPREnabled:!1,isOnDemandRevalidate:D,revalidateOnlyGenerated:A,responseGenerator:W,waitUntil:o.waitUntil,isMinimalMode:X});if(!w)return;if((null==r||null==(a=r.value)?void 0:a.kind)!==m.CachedRouteKind.APP_ROUTE)throw Object.defineProperty(Error(`Invariant: app-route received invalid cache entry ${null==r||null==(l=r.value)?void 0:l.kind}`),"__NEXT_ERROR_CODE",{value:"E701",enumerable:!1,configurable:!0});X||t.setHeader("x-nextjs-cache",D?"REVALIDATED":r.isMiss?"MISS":r.isStale?"STALE":"HIT"),C&&t.setHeader("Cache-Control","private, no-cache, no-store, max-age=0, must-revalidate");let s=(0,E.fromNodeOutgoingHttpHeaders)(r.value.headers);X&&w||s.delete(h.NEXT_CACHE_TAGS_HEADER),!r.cacheControl||t.getHeader("Cache-Control")||s.get("Cache-Control")||s.set("Cache-Control",(0,_.getCacheControlHeader)(r.cacheControl)),await (0,p.sendResponse)(q,z,new Response(r.value.body,{headers:s,status:r.value.status||200}));return}catch(t){if(t instanceof R.NoFallbackError||await $.onRequestError(e,t,{routerKind:"App Router",routePath:b,routeType:"route",revalidateReason:(0,d.getRevalidateReason)({isStaticGeneration:j,isOnDemandRevalidate:D})},!1,L),w)throw t;await (0,p.sendResponse)(q,z,new Response(null,{status:500}));return}finally{(()=>{if(!r)return;let e=t.statusCode;r.setAttributes({"http.status_code":e,"next.rsc":!1}),e&&e>=500&&(r.setStatus({code:s.SpanStatusCode.ERROR}),r.setAttribute("error.type",e.toString()));let n=H.getRootSpanAttributes();if(!n)return;if(n.get("next.span_type")!==u.BaseServerSpan.handleRequest)return console.warn(`Unexpected root span type '${n.get("next.span_type")}'. Please report this Next.js issue https://github.com/vercel/next.js`);let o=n.get("next.route")||b,a=`${k} ${o}`;r.setAttributes({"next.route":o,"http.route":o,"next.span_name":a}),r.updateName(a),i&&i!==r&&(i.setAttribute("http.route",o),i.updateName(a))})()}};if(B&&G)await Z(G,void 0);else{let t=H.getActiveScopeSpan();await H.withPropagatedContext(e.headers,()=>H.trace(u.BaseServerSpan.handleRequest,{spanName:`${k} ${g}`,kind:s.SpanKind.SERVER,attributes:{"http.method":k,"http.target":e.url}},e=>Z(e,t)),void 0,!B)}}e.s(["handler",0,O,"patchFetch",0,function(){return(0,o.patchFetch)({workAsyncStorage:g,workUnitAsyncStorage:T})},"routeModule",0,$,"serverHooks",0,f,"workAsyncStorage",0,g,"workUnitAsyncStorage",0,T])}];

//# sourceMappingURL=_1lj--f8._.js.map